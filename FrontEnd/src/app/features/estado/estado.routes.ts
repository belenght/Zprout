import { Routes } from '@angular/router';

// TODO: este feature todavia no tiene pages (list/form).
// Para completarlo, copiar la carpeta pages/ de features/tipo-semilla/
// y reemplazar TipoSemilla -> Estado / tipo-semilla -> estado.
//
// Una vez creadas las pages, descomentar esto y la linea correspondiente
// en app.routes.ts:
//
// export const estadoRoutes: Routes = [
//   { path: '', loadComponent: () => import('./pages/estado-list/estado-list').then(m => m.EstadoListComponent) },
//   { path: 'nuevo', loadComponent: () => import('./pages/estado-form/estado-form').then(m => m.EstadoFormComponent) },
//   { path: 'editar/:id', loadComponent: () => import('./pages/estado-form/estado-form').then(m => m.EstadoFormComponent) }
// ];

export const estadoRoutes: Routes = [];
