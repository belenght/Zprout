import { Routes } from '@angular/router';

// TODO: este feature todavia no tiene pages (list/form).
// Para completarlo, copiar la carpeta pages/ de features/tipo-semilla/
// y reemplazar TipoSemilla -> Rol / tipo-semilla -> rol.
//
// Una vez creadas las pages, descomentar esto y la linea correspondiente
// en app.routes.ts:
//
// export const rolRoutes: Routes = [
//   { path: '', loadComponent: () => import('./pages/rol-list/rol-list').then(m => m.RolListComponent) },
//   { path: 'nuevo', loadComponent: () => import('./pages/rol-form/rol-form').then(m => m.RolFormComponent) },
//   { path: 'editar/:id', loadComponent: () => import('./pages/rol-form/rol-form').then(m => m.RolFormComponent) }
// ];

export const rolRoutes: Routes = [];
