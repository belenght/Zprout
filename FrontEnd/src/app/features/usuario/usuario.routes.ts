import { Routes } from '@angular/router';

// TODO: este feature todavia no tiene pages (list/form).
// Para completarlo, copiar la carpeta pages/ de features/tipo-semilla/
// y reemplazar TipoSemilla -> Usuario / tipo-semilla -> usuario.
//
// Una vez creadas las pages, descomentar esto y la linea correspondiente
// en app.routes.ts:
//
// export const usuarioRoutes: Routes = [
//   { path: '', loadComponent: () => import('./pages/usuario-list/usuario-list').then(m => m.UsuarioListComponent) },
//   { path: 'nuevo', loadComponent: () => import('./pages/usuario-form/usuario-form').then(m => m.UsuarioFormComponent) },
//   { path: 'editar/:id', loadComponent: () => import('./pages/usuario-form/usuario-form').then(m => m.UsuarioFormComponent) }
// ];

export const usuarioRoutes: Routes = [];
